import { TableConfigModel } from 'models';

export const tableConfig: TableConfigModel = {
  columns: [
    {
      columnName: 'id',
    },
    {
      columnName: 'name',
    },
    {
      columnName: 'year',
    },
  ],
};
