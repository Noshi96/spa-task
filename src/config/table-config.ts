import { TableConfigModel } from 'models/TableConfigModel';

export const tableConfig: TableConfigModel = {
  columns: [
    {
      columnName: 'id',
    },
    {
      columnName: 'name',
    },
    {
      columnName: 'year',
    },
  ],
};
